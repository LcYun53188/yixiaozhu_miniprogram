# 益小助开发细节设计

## 一、文档说明

本文档用于支撑“益小助”项目从方案阶段进入实际开发阶段，重点补充数据库表结构设计、接口清单与开发任务拆分内容，便于团队快速分工、并行开发和后续联调。

本版本以“比赛可落地”为目标，优先保证核心业务闭环完整，即：

`登录 -> 发布 -> 审核 -> 匹配 -> 对接 -> 完成反馈`

## 二、开发范围定义

### 1. 本期必须完成

1. 微信登录与用户信息获取
2. 学号姓名验证与用户信息完善
3. 资源发布与需求发布
4. 资源/需求列表与详情查询
5. AI 分类、标签提取、文案优化
6. 管理员审核通过/驳回
7. 审核通过后的匹配推荐
8. 我的发布、我的需求、我的记录
9. 消息通知与状态流转

### 2. 本期建议完成

1. AI 风险识别
2. 图片辅助识别与 ISBN 扫码辅助
3. 管理员邀请码兑换
4. 管理员统计面板
5. 收藏功能

### 3. 本期可选加分

1. 管理员申请流程
2. 公益活动模块
3. 举报功能
4. 数据大屏

## 三、数据库表结构设计

## 3.1 设计原则

1. 优先满足比赛版本落地需求
2. 保持表结构清晰，避免过度复杂
3. 状态字段统一使用枚举值管理
4. 核心业务表支持后续扩展

## 3.2 用户表 `user`

用于存储普通用户、管理员和超级管理员的基础信息。

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| openid | varchar(64) | 微信用户唯一标识 |
| nickname | varchar(64) | 用户昵称 |
| avatar_url | varchar(255) | 头像地址 |
| phone | varchar(20) | 联系电话 |
| student_no | varchar(32) | 学号，可选 |
| real_name | varchar(32) | 真实姓名，可选 |
| college | varchar(64) | 学院信息 |
| identity_verified | tinyint | 是否完成学号姓名验证 |
| verified_at | datetime | 验证完成时间 |
| role | varchar(20) | 用户角色：user/admin/super_admin |
| credit_score | int | 信誉积分，默认 100 |
| rating_count | int | 收到评价次数 |
| status | varchar(20) | 账号状态：active/disabled |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

说明：

1. `openid` 用于登录身份识别
2. `role` 决定页面权限和接口访问权限
3. `phone` 可用于联系和对接

## 3.3 资源表 `resource`

用于存储用户发布的“我有资源”类信息。

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| user_id | bigint | 发布用户 ID |
| title | varchar(128) | 资源标题 |
| description | text | 原始描述 |
| polished_text | text | AI 优化后的描述 |
| category | varchar(32) | 分类，如旧书教材/闲置物资 |
| tags | varchar(512) | 标签集合，建议 JSON 字符串或逗号分隔 |
| image_urls | text | 图片地址集合 |
| contact_info | varchar(128) | 联系方式 |
| location_text | varchar(128) | 交接地点描述 |
| review_status | varchar(20) | 审核状态：pending/passed/rejected |
| flow_status | varchar(20) | 流转状态：pending_matching/matching/in_progress/completed |
| ai_risk_level | varchar(20) | AI 风险等级：low/medium/high |
| ai_risk_reason | varchar(255) | AI 风险原因 |
| published_at | datetime | 发布时间 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_flag | tinyint | 逻辑删除标记，0 否 1 是 |

## 3.4 需求表 `need`

用于存储用户发布的“我需要资源”类信息。

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| user_id | bigint | 发布用户 ID |
| title | varchar(128) | 需求标题 |
| description | text | 原始描述 |
| polished_text | text | AI 优化后的描述 |
| category | varchar(32) | 分类 |
| tags | varchar(512) | 标签集合 |
| urgency_level | varchar(20) | 紧急程度：low/medium/high |
| contact_info | varchar(128) | 联系方式 |
| review_status | varchar(20) | 审核状态：pending/passed/rejected |
| flow_status | varchar(20) | 流转状态：pending_matching/matching/in_progress/completed |
| ai_risk_level | varchar(20) | AI 风险等级 |
| ai_risk_reason | varchar(255) | AI 风险原因 |
| published_at | datetime | 发布时间 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
| deleted_flag | tinyint | 逻辑删除标记 |

## 3.5 匹配记录表 `match_record`

用于存储资源与需求之间的匹配结果。

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| resource_id | bigint | 资源 ID |
| need_id | bigint | 需求 ID |
| match_score | decimal(5,2) | 匹配分数，建议 0-100 |
| match_reason | varchar(255) | 匹配原因说明 |
| status | varchar(20) | 匹配状态：recommended/contacted/completed/invalid |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

说明：

1. 一个资源可以对应多条需求匹配记录
2. 一个需求也可以对应多条资源匹配记录

## 3.6 审核记录表 `review_record`

用于记录管理员审核动作。

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| target_type | varchar(20) | 审核对象类型：resource/need/admin_apply |
| target_id | bigint | 审核对象 ID |
| review_status | varchar(20) | 审核结果：passed/rejected |
| review_reason | varchar(255) | 驳回原因或审核备注 |
| reviewer_id | bigint | 审核人 ID |
| created_at | datetime | 创建时间 |

## 3.7 消息表 `message`

用于向用户推送审核通知、匹配通知和状态变化通知。

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| user_id | bigint | 接收用户 ID |
| message_type | varchar(32) | 消息类型：review/match/status/system |
| title | varchar(128) | 消息标题 |
| content | varchar(500) | 消息内容 |
| related_type | varchar(20) | 关联对象类型：resource/need/match |
| related_id | bigint | 关联对象 ID |
| is_read | tinyint | 是否已读，0 否 1 是 |
| created_at | datetime | 创建时间 |

## 3.8 收藏表 `favorite`

用于记录用户收藏的资源或需求。

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| user_id | bigint | 用户 ID |
| target_type | varchar(20) | 收藏对象类型：resource/need |
| target_id | bigint | 收藏对象 ID |
| created_at | datetime | 创建时间 |

## 3.9 管理员申请表 `admin_apply`

如果后续做管理员申请流程，可使用该表。

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| user_id | bigint | 申请人 ID |
| apply_reason | varchar(255) | 申请原因 |
| status | varchar(20) | 审核状态：pending/passed/rejected |
| reviewer_id | bigint | 审核人 ID |
| review_reason | varchar(255) | 审核备注 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

## 3.10 状态枚举建议

### 审核状态

- `pending`：待审核
- `passed`：审核通过
- `rejected`：审核驳回

### 流转状态

- `pending_matching`：待匹配
- `matching`：匹配中
- `in_progress`：对接中
- `completed`：已完成

### 用户角色

- `user`：普通用户
- `admin`：管理员
- `super_admin`：超级管理员

## 3.11 管理员邀请码表 `admin_invite_code`

用于记录一次性管理员邀请码的生成、使用和停用状态。

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| invite_code | varchar(64) | 邀请码 |
| used | tinyint | 是否已使用 |
| disabled | tinyint | 是否已停用 |
| used_by | bigint | 使用用户 ID |
| used_at | datetime | 使用时间 |
| created_at | datetime | 创建时间 |

## 四、接口清单

## 4.1 接口设计规范

1. 接口统一前缀：`/api`
2. 返回结构统一为：`code + message + data`
3. 需要登录的接口统一校验 token
4. 管理接口统一校验管理员角色

标准返回示例：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

## 4.2 用户模块接口

### 1. 微信登录

- 方法：`POST`
- 路径：`/api/user/login`
- 说明：根据小程序登录凭证换取用户身份并返回登录态

### 2. 获取当前用户信息

- 方法：`GET`
- 路径：`/api/user/profile`
- 说明：获取当前登录用户信息

### 3. 更新用户信息

- 方法：`PUT`
- 路径：`/api/user/profile`
- 说明：更新昵称、手机号、学院等信息

### 4. 学号姓名验证

- 方法：`POST`
- 路径：`/api/user/identity/verify`
- 说明：提交学号、真实姓名和学院信息，完成身份验证

### 5. 管理员邀请码兑换

- 方法：`POST`
- 路径：`/api/user/admin-invite/redeem`
- 说明：验证通过后使用一次性邀请码升级为管理员

## 4.3 资源模块接口

### 1. 创建资源

- 方法：`POST`
- 路径：`/api/resource/create`
- 说明：创建“我有资源”记录

### 2. 更新资源

- 方法：`PUT`
- 路径：`/api/resource/update/:id`
- 说明：修改资源信息

### 3. 删除资源

- 方法：`DELETE`
- 路径：`/api/resource/delete/:id`
- 说明：逻辑删除资源

### 4. 资源列表查询

- 方法：`GET`
- 路径：`/api/resource/list`
- 说明：支持分页、分类、关键词、状态筛选

### 5. 资源详情

- 方法：`GET`
- 路径：`/api/resource/detail/:id`
- 说明：获取资源详情及相关推荐

### 6. 我的资源列表

- 方法：`GET`
- 路径：`/api/resource/my-list`
- 说明：获取当前用户发布的资源记录

## 4.4 需求模块接口

### 1. 创建需求

- 方法：`POST`
- 路径：`/api/need/create`
- 说明：创建“我需要资源”记录

### 2. 更新需求

- 方法：`PUT`
- 路径：`/api/need/update/:id`
- 说明：修改需求信息

### 3. 删除需求

- 方法：`DELETE`
- 路径：`/api/need/delete/:id`
- 说明：逻辑删除需求

### 4. 需求列表查询

- 方法：`GET`
- 路径：`/api/need/list`
- 说明：支持分页、分类、关键词、状态筛选

### 5. 需求详情

- 方法：`GET`
- 路径：`/api/need/detail/:id`
- 说明：获取需求详情及相关推荐

### 6. 我的需求列表

- 方法：`GET`
- 路径：`/api/need/my-list`
- 说明：获取当前用户发布的需求记录

## 4.5 AI 模块接口

### 1. 文本分类

- 方法：`POST`
- 路径：`/api/ai/classify`

### 2. 标签提取

- 方法：`POST`
- 路径：`/api/ai/tags`

### 3. 文案优化

- 方法：`POST`
- 路径：`/api/ai/polish`

### 4. 风险识别

- 方法：`POST`
- 路径：`/api/ai/moderation`

### 5. 图片辅助识别

- 方法：`POST`
- 路径：`/api/ai/image-assist`

### 6. ISBN 扫码辅助

- 方法：`POST`
- 路径：`/api/ai/isbn-assist`

### 7. 智能匹配

- 方法：`POST`
- 路径：`/api/ai/match`

## 4.6 审核模块接口

### 1. 待审核列表

- 方法：`GET`
- 路径：`/api/admin/review/list`
- 说明：管理员获取待审核资源和需求

### 2. 审核通过

- 方法：`POST`
- 路径：`/api/admin/review/pass`
- 说明：审核通过并触发匹配逻辑

### 3. 审核驳回

- 方法：`POST`
- 路径：`/api/admin/review/reject`
- 说明：审核驳回并写入驳回原因

## 4.7 匹配模块接口

### 1. 获取匹配结果

- 方法：`GET`
- 路径：`/api/match/list`
- 说明：根据资源 ID 或需求 ID 查询匹配结果

### 2. 更新匹配状态

- 方法：`PUT`
- 路径：`/api/match/status/:id`
- 说明：更新为 contacted/completed/invalid

### 3. 获取我的匹配记录

- 方法：`GET`
- 路径：`/api/match/my-list`
- 说明：获取当前用户相关匹配记录

## 4.8 消息模块接口

### 1. 消息列表

- 方法：`GET`
- 路径：`/api/message/list`

### 2. 消息已读

- 方法：`PUT`
- 路径：`/api/message/read/:id`

## 4.9 收藏模块接口

### 1. 添加收藏

- 方法：`POST`
- 路径：`/api/favorite/create`

### 2. 取消收藏

- 方法：`DELETE`
- 路径：`/api/favorite/delete/:id`

### 3. 我的收藏列表

- 方法：`GET`
- 路径：`/api/favorite/my-list`

## 4.10 管理后台统计接口

### 1. 数据概览

- 方法：`GET`
- 路径：`/api/admin/dashboard/summary`
- 说明：返回资源数、需求数、待审核数、已完成数等统计信息

### 2. 用户列表

- 方法：`GET`
- 路径：`/api/admin/user/list`

### 3. 更新用户角色

- 方法：`PUT`
- 路径：`/api/admin/user/role/:id`

## 五、建议请求参数结构

## 5.1 创建资源请求示例

```json
{
  "title": "高等数学教材转赠",
  "description": "有几本高数教材，九成新，适合大一学生",
  "category": "旧书教材",
  "tags": ["教材", "高数", "大一"],
  "imageUrls": [
    "https://example.com/1.jpg"
  ],
  "contactInfo": "微信：abc123",
  "locationText": "图书馆门口"
}
```

## 5.2 创建需求请求示例

```json
{
  "title": "求高数复习资料",
  "description": "准备期末考试，希望获取高等数学复习资料",
  "category": "旧书教材",
  "tags": ["高数", "复习", "期末考试"],
  "urgencyLevel": "high",
  "contactInfo": "微信：stu001"
}
```

## 5.3 审核通过请求示例

```json
{
  "targetType": "resource",
  "targetId": 101
}
```

## 5.4 审核驳回请求示例

```json
{
  "targetType": "need",
  "targetId": 202,
  "reviewReason": "描述信息不完整，请补充具体需求内容"
}
```

## 六、开发任务拆分表

## 6.1 角色分工建议

如果团队为 4 人，推荐分工如下：

1. 前端开发 1：用户端页面与交互
2. 前端开发 2：管理员端页面与状态流转
3. 后端开发：接口、数据库、权限、审核、匹配逻辑
4. AI 与文档：AI 接口接入、Prompt、测试数据、PDF/PPT/视频

如果团队只有 3 人，可将后台管理和 AI 接入交由后端兼顾。

## 6.2 按模块拆分任务

| 模块 | 子任务 | 负责人建议 | 优先级 |
|---|---|---|---|
| 项目初始化 | 创建前后端项目骨架、配置数据库、基础路由 | 后端/全员 | 高 |
| 登录模块 | 微信登录、获取用户信息、登录态管理 | 前端+后端 | 高 |
| 首页模块 | 首页布局、分类入口、搜索框、列表展示 | 前端 | 高 |
| 发布模块 | 发布资源、发布需求、表单校验、图片上传 | 前端+后端 | 高 |
| AI 模块 | 分类、标签、文案优化、风险识别 API 接入 | AI/后端 | 高 |
| 资源模块 | 资源列表、详情、我的资源 | 前端+后端 | 高 |
| 需求模块 | 需求列表、详情、我的需求 | 前端+后端 | 高 |
| 审核模块 | 待审核列表、通过/驳回、审核记录 | 后端+管理员前端 | 高 |
| 匹配模块 | 匹配计算、匹配展示、状态更新 | 后端+前端 | 高 |
| 消息模块 | 消息通知列表、已读状态 | 前端+后端 | 中 |
| 收藏模块 | 收藏、取消收藏、我的收藏 | 前端+后端 | 中 |
| 统计模块 | 管理员概览统计 | 后端+管理员前端 | 中 |
| 测试数据 | 构造资源、需求、审核、匹配样例数据 | AI/文档 | 高 |
| 比赛材料 | 介绍文档、演示视频、PPT | 全员 | 高 |

## 6.3 按时间阶段拆分

### 第一阶段：基础框架搭建

目标：项目能跑起来，数据库能连通，页面能进入。

任务：

1. 创建前端小程序项目
2. 创建后端项目和数据库连接
3. 设计基础目录结构
4. 完成登录和角色识别

### 第二阶段：核心业务开发

目标：实现发布、审核、匹配的主流程。

任务：

1. 完成资源与需求发布接口
2. 完成列表和详情接口
3. 完成管理员审核接口
4. 完成匹配记录生成逻辑
5. 完成消息通知基础功能

### 第三阶段：AI 能力接入

目标：让系统具备比赛亮点。

任务：

1. 接入文本分类
2. 接入标签提取
3. 接入文案优化
4. 接入风险识别
5. 生成匹配原因说明

### 第四阶段：联调与完善

目标：保证系统能完整演示。

任务：

1. 前后端联调
2. 修复状态流转问题
3. 补充空页面和异常提示
4. 完善管理员端展示
5. 准备测试账号和测试数据

### 第五阶段：比赛交付

目标：形成完整参赛材料。

任务：

1. 录制演示视频
2. 制作项目介绍 PDF
3. 制作答辩 PPT
4. 整理项目亮点和分工说明

## 6.4 最小可用版本任务清单

如果时间非常紧，建议优先只做下面这些：

1. 微信登录
2. 首页分类与列表
3. 发布资源/需求
4. AI 标签提取与文案优化
5. 管理员审核通过/驳回
6. 匹配结果展示
7. 我的发布/我的需求

这 7 项完成后，项目就已经具备较完整的演示价值。

## 七、开发前检查清单

正式开工前建议确认以下事项：

1. 是否已确定最终技术栈
2. 是否已明确每位成员分工
3. 是否已准备测试数据
4. 是否已确定 AI 接口来源
5. 是否已确定演示主流程
6. 是否已确定管理员账号方案
7. 是否已确定数据库表结构与状态枚举

## 八、总结

本开发细节设计文档重点解决“方案能讲，但开发难落地”的问题。通过补充字段级数据库设计、接口清单和任务拆分表，可以让团队更快进入编码阶段，并减少后续联调和返工成本。对于比赛版本而言，建议始终围绕“发布、审核、匹配、完成反馈”这一主流程推进，在保证核心闭环完成的基础上，再逐步补充 AI 和管理端亮点功能。

## 九、当前实现同步补充

本节用于同步当前代码中已经落地的新增功能，便于后续开发、答辩材料和数据库设计保持一致。

### 9.1 新增开发范围

当前版本在原有核心闭环基础上，新增以下内容：

1. 发布页图片添加、预览和删除
2. 详情页图片展示和预览
3. 发布页 ISBN 扫码辅助和图片识别辅助
4. 设置页学号姓名验证与管理员邀请码兑换
5. 匹配页标记已联系
6. 双方星级评价、文字评价和评价标签
7. 用户信誉积分与评价次数统计
8. 评价结果消息通知
9. Docker Compose 开发环境
10. phpMyAdmin 数据库管理入口

### 9.2 用户表补充字段

用户表 `user` 补充身份与信誉相关字段：

| 字段名 | 类型 | 说明 |
|---|---|---|
| identity_verified | tinyint | 是否已完成学号姓名验证 |
| verified_at | datetime | 验证通过时间 |
| credit_score | int | 用户信誉积分，默认 100，范围建议 0 到 150 |
| rating_count | int | 用户收到评价次数 |

说明：

1. 新用户默认信誉分为 100 分
2. 用户每收到一次评价，`rating_count` 增加 1
3. 身份验证通过后，`identity_verified` 设为 1 并记录 `verified_at`
4. 信誉分可用于后续推荐排序、风险提示、用户可信度展示

### 9.3 评价表 `feedback`

用于记录资源方和需求方完成对接后的互评数据。

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| match_id | bigint | 匹配记录 ID |
| from_user_id | bigint | 评价人 ID |
| to_user_id | bigint | 被评价人 ID |
| rating | tinyint | 星级评分，1 到 5 |
| content | varchar(500) | 文字评价内容 |
| tags | varchar(255) | 评价标签集合，建议 JSON 字符串 |
| credit_delta | int | 本次评价带来的信誉分变化 |
| created_at | datetime | 创建时间 |

约束建议：

1. 同一用户对同一匹配记录只能评价一次
2. 只有匹配双方可以提交评价
3. 不能对自己进行评价
4. 评价提交后匹配状态可更新为 `completed`

### 9.4 信誉积分规则

当前演示版本采用简单可解释的积分规则：

| 评分 | 信誉分变化 |
|---|---|
| 5 星 | +5 |
| 4 星 | +3 |
| 3 星 | +1 |
| 2 星 | -3 |
| 1 星 | -6 |

信誉分下限为 0，上限为 150。该规则适合比赛演示阶段使用，后续可结合完成次数、投诉记录、管理员处理记录和评价文本风险识别进一步优化。

### 9.5 新增评价接口

#### 1. 提交评价

- 方法：`POST`
- 路径：`/api/feedback/create`
- 说明：匹配双方在完成对接后提交评价

请求示例：

```json
{
  "matchId": 301,
  "rating": 5,
  "content": "沟通及时，物品情况真实",
  "tags": ["沟通及时", "物品真实"]
}
```

#### 2. 我的评价记录

- 方法：`GET`
- 路径：`/api/feedback/my-list`
- 说明：获取当前用户发出或收到的评价记录

#### 3. 用户信誉信息

- 方法：`GET`
- 路径：`/api/feedback/credit/:userId`
- 说明：获取指定用户信誉分、评价次数和平均评分

### 9.6 匹配模块补充

匹配页除了展示匹配度和推荐理由外，当前增加以下操作：

1. `标记已联系`：将匹配状态更新为 `contacted`
2. `评价对方`：打开评价面板，提交评分、文字评价和标签
3. 展示资源方和需求方昵称、信誉分

匹配记录状态仍使用：

- `recommended`：系统推荐
- `contacted`：已联系
- `completed`：已完成
- `invalid`：无效

### 9.7 图片添加功能补充

发布页当前支持最多添加 3 张图片。小程序端使用 `wx.chooseMedia` 从相册或相机选择图片，并将临时图片路径写入 `imageUrls` 字段。详情页根据 `imageUrls` 展示图片并支持预览。

正式部署时建议增加后端上传接口或接入对象存储，流程为：

`选择图片 -> 上传到对象存储 -> 返回图片 URL -> 保存到 image_urls 字段 -> 详情页回显`

### 9.8 ISBN 扫码与图片识别补充

发布页当前支持三种智能录入方式：

1. `扫码识别 ISBN`：针对教材场景快速生成标题、分类和标签建议
2. `相机识别物品`：拍照后调用 `/api/ai/image-assist`
3. `上传图片识别`：从相册选择图片后调用 `/api/ai/image-assist`

在正式启用真实图像识别服务前，后端会使用规则兜底逻辑返回识别建议，保证比赛演示可用。

### 9.9 管理员邀请码补充

当前版本已增加一次性管理员邀请码机制。普通用户完成学号姓名验证后，可在设置页输入邀请码兑换管理员权限；邀请码一经使用即失效，并记录使用人和使用时间。

### 9.10 Docker 开发环境补充

当前项目已提供 Docker Compose 开发环境，包含：

1. `backend`：Node.js + Express 后端服务
2. `mysql`：MySQL 8.4 数据库
3. `phpmyadmin`：数据库可视化管理工具

启动命令：

```powershell
docker compose up -d --build
```

访问地址：

```text
后端 API: http://localhost:3000/api/health
数据库检测: http://localhost:3000/api/health/db
phpMyAdmin: http://localhost:8080
MySQL: localhost:3306
```

如果 Docker Hub 网络不稳定，可通过 `.env` 中的 `NODE_IMAGE`、`MYSQL_IMAGE`、`PHPMYADMIN_IMAGE` 切换镜像源。
